{
    "name": "event-service",
    "version": "0.0.1",
    "private": true,
    "workspaces": [
        "event-domain/**",
        "event-boot/**",
        "event-api/**",
        "event-spi/**"
    ],
    "scripts": {
        "prepare": "npm run compile",
        "compile": "tsc --build --force tsconfig.json",
        "start": "node event-boot"
    }
}
